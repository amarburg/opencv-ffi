# Generated by mkrf
require 'rake/clean'

CLEAN.include('*.o')
CLOBBER.include('libopencvffi.so', 'mkrf.log')

SRC = FileList['*.cpp']
OBJ = SRC.ext('o')
CC = 'g++'

ADDITIONAL_OBJECTS = ''

LDSHARED = "gcc -shared "

LIBPATH =  "-L/home/amarburg/.rvm/rubies/ruby-1.9.2-p290/lib "

INCLUDES = "-I/home/amarburg/.rvm/rubies/ruby-1.9.2-p290/include/ruby-1.9.1 -I/home/amarburg/.rvm/rubies/ruby-1.9.2-p290/include/ruby-1.9.1/x86_64-linux -I/home/amarburg/.rvm/rubies/ruby-1.9.2-p290/lib/ruby/1.9.1/x86_64-linux -I/home/amarburg/.rvm/rubies/ruby-1.9.2-p290/lib/ruby/site_ruby/1.9.1 -I."

LIBS = "-lpthread -lrt -ldl -lcrypt -lm"

CFLAGS = "-fPIC  -O3 -ggdb -Wextra -Wno-unused-parameter -Wno-parentheses -Wpointer-arith -Wwrite-strings -Wno-missing-field-initializers -Wno-long-long  -fPIC  "

RUBYARCHDIR = "#{ENV["RUBYARCHDIR"]}"
LIBRUBYARG_SHARED = "-Wl,-R -Wl,/home/amarburg/.rvm/rubies/ruby-1.9.2-p290/lib -L/home/amarburg/.rvm/rubies/ruby-1.9.2-p290/lib -lruby"

task :default => ['libopencvffi.so']

rule '.o' => '.cpp' do |t|
  sh "#{CC} #{CFLAGS} #{INCLUDES} -c #{t.source}"
end

desc "Build this extension"
file 'libopencvffi.so' => OBJ do
  sh "#{LDSHARED} #{LIBPATH} -o libopencvffi.so #{OBJ} #{ADDITIONAL_OBJECTS} #{LIBS} #{LIBRUBYARG_SHARED}"
end

desc "Install this extension"
task :install => 'libopencvffi.so' do
  makedirs "#{RUBYARCHDIR}"
  install "libopencvffi.so", "#{RUBYARCHDIR}"
end


